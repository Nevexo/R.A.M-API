{
    "api": {
        "port": 3000,
        "base_endpoint": "/api",
        "endpoints": {
            "uplink": "/uplink",
            "aggregates": "/aggregates"
        }
    },
    "authentication": {
        "tokens": [""]
    }
}